apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "navidrome-music"
  namespace: navidrome
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "100Gi"