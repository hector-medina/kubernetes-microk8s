apiVersion: v1
kind: PersistentVolume
metadata:
  name: "radarr-config"
  namespace: radarr
  labels:
    type: "local"
spec:
  storageClassName: "manual"
  capacity:
    storage: "1Gi"
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/media/sda1/radarr/config"