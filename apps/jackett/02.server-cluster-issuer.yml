apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  namespace: jackett
  name: jackett-letsencrypt
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: hmedcab@gmail.com
    privateKeySecretRef:
       name: jackett-letsencrypt
    solvers:
    - http01:
        ingress:
          class: public
