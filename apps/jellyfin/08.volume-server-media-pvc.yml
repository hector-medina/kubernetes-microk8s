apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "jellyfin-media"
  namespace: jellyfin
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "500Gi"